func main() void {
  let a int = 4;
  if(a > 3) {
    fn print("if-else stmt, if block exec.");
  }
  else {
    fn print("if-else stmt, else block exec.");
  }
  while(a > 1) {
    fn print("while stmt, a = ", a);
    a = a - 1;
  }
  fn print("a should be 1 : ", a);
  for(a = 10; a > 1; a = a - 1) {
    fn print("for stmt, a = ", a);
  }
  return;
}
